import React from 'react';

export const FilterContext = React.createContext({
  filter: '',
  setFilter: () => {},
});

export const PromoFilterContext = React.createContext({
  filter: '',
  setFilter: () => {},
});